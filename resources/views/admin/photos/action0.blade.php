<span class="photo-edit">
    <a href="{{ route('admin.photos.edit',  $photo->id ) }}" data-photoid="{{ $photo->id }}" class="btn btn-sm btn-flat btn-primary">{{ trans('admin_message.photos.edit') }}</a>
</span>